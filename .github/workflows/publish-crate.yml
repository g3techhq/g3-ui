name: Publish crate

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Run cargo publish --dry-run instead of publishing
        required: true
        default: true
        type: boolean

concurrency:
  group: publish-crate
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  publish:
    name: Publish g3-ui
    runs-on: ubuntu-latest
    environment: crates-io

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87

      - name: Show toolchain
        run: |
          rustc --version
          cargo --version

      - name: Publish crate
        shell: bash
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail

          if [[ "${DRY_RUN}" == "true" ]]; then
            echo "Listing package files for g3-ui; full dry-run requires g3-route-transitions to exist on crates.io first."
            cargo package --list
            exit 0
          fi

          if [[ -z "${CARGO_REGISTRY_TOKEN:-}" ]]; then
            echo "CARGO_REGISTRY_TOKEN is required when dry_run is false" >&2
            exit 1
          fi

          cargo publish
